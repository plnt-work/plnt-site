# Copy to .env (gitignored) for local dev. Do NOT commit real values.

# Where the frontend fetches /v1/models and /v1/chat/completions from.
# Defaults to https://playground.plnt.work in production if unset.
PUBLIC_PLNT_ENDPOINT=http://localhost:8080
